module.exports = {
  // authorize: require('./authorize.middleware'),
  // authenticated: require('./.middleware'),
  paging: require('./paging.middleware'),
  validation: require('./validation.middleware'),
};
